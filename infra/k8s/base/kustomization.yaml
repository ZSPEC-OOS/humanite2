apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - orchestration.yaml
  - gateway.yaml

commonLabels:
  app.kubernetes.io/part-of: humanite
  app.kubernetes.io/managed-by: kustomize
