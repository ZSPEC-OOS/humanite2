import type { CorpusItem } from '../types'

export const TECHNICAL_CORPUS: CorpusItem[] = [
  {
    id: 'technical-01',
    domain: 'technical',
    input: 'To install the CLI, run `npm install -g example-cli@2.4.1`, which requires Node.js 18 or later. Once installed, initialize a project with `example-cli init`, which creates a config file at `.example/config.yaml`. The default HTTP timeout is 30000 milliseconds; increase it via the `--timeout` flag if requests to slower endpoints time out. Version 2.4.1 fixed a memory leak present in versions 2.3.0 through 2.3.4, so upgrading from any version in that range is strongly recommended before running the tool against production data.',
    mandatoryFacts: ['npm install -g example-cli@2.4.1', 'Node.js 18', '.example/config.yaml', '30000 milliseconds', '--timeout', '2.3.0 through 2.3.4'],
    prohibitedChanges: ['example-cli@4.2.1', 'Node.js 8', 'default HTTP timeout is 3000 milliseconds', 'versions 3.3.0 through 3.3.4'],
    expectedProperties: { minWordCount: 60, maxWordCount: 140, notes: 'Version numbers and code identifiers must survive character-for-character.' },
  },
  {
    id: 'technical-02',
    domain: 'technical',
    input: 'The service exposes a REST endpoint at `POST /v1/jobs`, which accepts a JSON body and returns a 202 status code along with a `job_id` once the request is accepted. Clients should poll `GET /v1/jobs/{job_id}` no more than once every 2 seconds; polling more frequently than that may trigger a 429 rate-limit response. Job processing typically completes within 90 seconds for payloads under 10 MB, but can take up to 8 minutes for payloads approaching the 50 MB maximum accepted size.',
    mandatoryFacts: ['POST /v1/jobs', '202', 'job_id', 'GET /v1/jobs/{job_id}', 'every 2 seconds', '429', '90 seconds', '10 MB', '8 minutes', '50 MB'],
    prohibitedChanges: ['returns a 500 status code', 'polling... once every 20 seconds', 'up to 50 MB maximum accepted size' /* corrupted variant reusing the size figure incorrectly */],
    expectedProperties: { minWordCount: 60, maxWordCount: 150 },
  },
  {
    id: 'technical-03',
    domain: 'technical',
    input: 'Database connection pooling should be configured with a minimum of 5 connections and a maximum of 50 per application instance; setting the maximum above 100 has been observed to overwhelm the database\'s own connection limit of 500 when 10 or more instances run concurrently. The default idle timeout of 600 seconds releases unused connections back to the pool, and reducing it below 60 seconds can cause excessive connection churn under bursty traffic. Connection acquisition should time out after 5000 milliseconds rather than blocking indefinitely.',
    mandatoryFacts: ['minimum of 5 connections', 'maximum of 50', 'above 100', 'connection limit of 500', '10 or more instances', '600 seconds', 'below 60 seconds', '5000 milliseconds'],
    prohibitedChanges: ['a maximum of 500 per application instance', 'connection limit of 5,000', 'time out after 50000 milliseconds'],
    expectedProperties: { minWordCount: 60, maxWordCount: 160 },
  },
  {
    id: 'technical-04',
    domain: 'technical',
    input: 'The build pipeline runs 3 stages in sequence: lint, test, and deploy. The lint stage must complete in under 2 minutes or the pipeline fails fast without proceeding to the test stage, which typically runs the full suite of 1,240 tests in 6 to 9 minutes. Deployment is gated behind a manual approval step and, once approved, completes within 4 minutes for a standard rollout or up to 12 minutes for a rollout requiring a database migration. Failed pipelines send an alert to the on-call engineer within 30 seconds of the failing step.',
    mandatoryFacts: ['3 stages', 'under 2 minutes', '1,240 tests', '6 to 9 minutes', 'within 4 minutes', 'up to 12 minutes', '30 seconds'],
    prohibitedChanges: ['runs 8 stages in sequence', 'suite of 12,400 tests', 'completes within 40 minutes for a standard rollout'],
    expectedProperties: { minWordCount: 60, maxWordCount: 160 },
  },
  {
    id: 'technical-05',
    domain: 'technical',
    input: 'The cache layer uses an LRU eviction policy with a maximum size of 256 MB per node, spread across a 12-node cluster for a total effective capacity of roughly 3 GB before replication overhead. Cache hit rate under typical production load sits at 91 percent, and TTL for most keys is set to 3600 seconds, except session tokens, which expire after 900 seconds. Falling below an 80 percent hit rate for more than 5 consecutive minutes triggers an automatic alert to the platform team.',
    mandatoryFacts: ['256 MB per node', '12-node cluster', 'roughly 3 GB', '91 percent', '3600 seconds', '900 seconds', '80 percent', '5 consecutive minutes'],
    prohibitedChanges: ['maximum size of 2560 MB per node', 'a 2-node cluster', 'TTL for most keys is set to 36000 seconds'],
    expectedProperties: { minWordCount: 60, maxWordCount: 160 },
  },
  {
    id: 'technical-06',
    domain: 'technical',
    input: 'The recommended TLS configuration disables all versions below TLS 1.2 and prefers TLS 1.3 where the client supports it. Certificates should be rotated every 90 days at most, and the current implementation rotates them automatically every 60 days with a 7-day overlap window to avoid downtime during rotation. Cipher suites relying on RC4 or 3DES must never be enabled, since both are considered cryptographically broken for this use case.',
    mandatoryFacts: ['TLS 1.2', 'TLS 1.3', 'every 90 days', 'every 60 days', '7-day overlap window', 'RC4', '3DES'],
    prohibitedChanges: ['prefers TLS 1.0 where the client supports it', 'rotates them automatically every 600 days', 'RC4 or 3DES must always be enabled'],
    expectedProperties: { minWordCount: 60, maxWordCount: 140, notes: 'Modal "must never be enabled" is safety-critical and must not flip to permissive.' },
  },
  {
    id: 'technical-07',
    domain: 'technical',
    input: 'Load testing against the checkout API found that throughput plateaus at 1,800 requests per second, beyond which p99 latency rises sharply from 120 milliseconds to over 900 milliseconds. The bottleneck was traced to a single-threaded validation step consuming 4 CPU cores at peak; parallelizing that step across 8 worker threads in a follow-up test raised the plateau to 3,100 requests per second while keeping p99 latency under 200 milliseconds.',
    mandatoryFacts: ['1,800 requests per second', '120 milliseconds', 'over 900 milliseconds', '4 CPU cores', '8 worker threads', '3,100 requests per second', 'under 200 milliseconds'],
    prohibitedChanges: ['throughput plateaus at 18,000 requests per second', 'p99 latency... falls sharply', 'raised the plateau to 310 requests per second'],
    expectedProperties: { minWordCount: 60, maxWordCount: 150 },
  },
  {
    id: 'technical-08',
    domain: 'technical',
    input: 'The mobile app\'s crash-free session rate improved from 97.2 percent to 99.4 percent after the release of version 5.1.0, which fixed a null-pointer exception affecting roughly 1 in 400 sessions on Android devices running API level 29 or lower. The fix required no schema migration and was rolled out to 100 percent of users within 48 hours via a staged rollout that started at 10 percent and doubled every 6 hours.',
    mandatoryFacts: ['97.2 percent', '99.4 percent', 'version 5.1.0', '1 in 400 sessions', 'API level 29', '48 hours', '10 percent', 'every 6 hours'],
    prohibitedChanges: ['crash-free session rate declined from 97.2 percent', 'version 1.5.0', 'API level 92', 'started at 100 percent and doubled every 6 hours'],
    expectedProperties: { minWordCount: 60, maxWordCount: 150 },
  },
  {
    id: 'technical-09',
    domain: 'technical',
    input: 'The recommended backup schedule takes a full snapshot every 24 hours and an incremental snapshot every 4 hours in between, retaining 7 daily snapshots and 4 weekly snapshots at any given time. Restore testing performed quarterly confirmed that a full restore from the most recent daily snapshot completes in under 35 minutes for a 200 GB dataset, comfortably inside the 1-hour recovery time objective the team committed to.',
    mandatoryFacts: ['every 24 hours', 'every 4 hours', '7 daily snapshots', '4 weekly snapshots', 'under 35 minutes', '200 GB', '1-hour recovery time objective'],
    prohibitedChanges: ['a full snapshot every 240 hours', 'retaining 70 daily snapshots', 'completes in under 3 minutes and 5 hours' /* nonsensical corruption of the timing figure */],
    expectedProperties: { minWordCount: 60, maxWordCount: 150 },
  },
  {
    id: 'technical-10',
    domain: 'technical',
    input: 'Static analysis flagged 214 findings across the codebase, of which 18 were classified as high severity, primarily unvalidated input reaching a SQL query builder in 3 separate modules. All 18 high-severity findings were remediated within the 2-week window mandated by the security policy, and a regression test was added for each. The remaining 196 medium- and low-severity findings were triaged, with 140 scheduled for the next quarterly cleanup and 56 accepted as false positives after manual review.',
    mandatoryFacts: ['214 findings', '18', 'high severity', '3 separate modules', '2-week window', '196', '140', '56'],
    prohibitedChanges: ['flagged 24 findings', 'of which 81 were classified as high severity', 'remediated within the 20-week window'],
    expectedProperties: { minWordCount: 60, maxWordCount: 160 },
  },
]
